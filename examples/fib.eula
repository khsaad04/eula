
main :: fn {
    // Note that all variables are zero-initialized by default at
    // declaration. So, we didn't need to explicitly assign 0 to a and c
    a, b, c: int;
    b = 1;

    while a < 1_000_000 {
        printf("%d\n", a);
        c = a + b;
        a = b;
        b = c;
    }
}
